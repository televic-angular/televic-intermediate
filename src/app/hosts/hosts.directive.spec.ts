import { HostsDirective } from './hosts.directive';

describe('HostsDirective', () => {
  it('should create an instance', () => {
    const directive = new HostsDirective();
    expect(directive).toBeTruthy();
  });
});
